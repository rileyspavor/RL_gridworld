# Experiment Inbox

Use this file as a scratchpad for incoming findings from the separate experiment agent.

## How to log updates
For each new result, append:
- timestamp
- run/config name
- key metrics
- anything surprising
- whether the report draft needs to change

---

## Incoming results log

### Waiting for runs
- No completed experiment artifacts have been logged here yet.
- Current leading hypothesis only: **PPO + frontier_features + dense_coverage** on `core_generalization`.
- This is a starting point, not a final empirical conclusion.
